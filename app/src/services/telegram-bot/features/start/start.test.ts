import '../../test-utils'
import { prismaMock } from '~/../test/prisma-mock'

describe('start command (onboarding)', () => {
  it.todo('user found and displayed feature menu')

  it.todo('invalid onboarding token')

  it.todo('valid onboarding token')
})
